apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`flask.aws.elysium-space.com`)  # Replace with your domain
      kind: Rule
      services:
        - name: {{ .Release.Name }}
          port: 80
